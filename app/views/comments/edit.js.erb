$("#comment-content<%= @comment.id %>").html("<%= escape_javascript(render 'form', comment: @comment, display_comment_field: true) %>");
$('.toggle-reply').on('click', function(e) {
  e.preventDefault();
  $(e.target).parents("ul").next('.comment-reply').toggleClass('hidden');
});