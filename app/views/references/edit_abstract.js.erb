newForm = "<%= escape_javascript(render('papers/abstract_form', paper: @reference.paper)) %>";
abstractDiv = $("#edit-abstract-<%= @reference.id %>");
abstractDiv.data("undo-form", abstractDiv.html());

abstractDiv.html(newForm);
cancelLink = abstractDiv.find("a.cancel-abstract-form");
cancelLink.toggleClass('hidden', false);
