<% if current_user.homepage.lists.include? list %>
  <%= link_to 'Unpin from homepage', pins_path(list), method: :delete %>
<% else %>
  <%= link_to 'Pin to homepage', pins_path(list), method: :post %>
<% end %>
