<% if current_user.homepage.lists.include? list %>
  <%= link_to button_tag('Unpin from homepage'), pin_path(list), method: :delete %>
<% else %>
  <%= form_tag pins_path do %>
    <%= text_field_tag 'id', list.id, hidden: true %>
    <%= submit_tag 'Pin to homepage' %>
  <% end %>
<% end %>
